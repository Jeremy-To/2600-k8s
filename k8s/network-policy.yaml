apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-external-to-pod1
  namespace: external
spec:
  podSelector:
    matchLabels:
      app: pod1-webapp
  policyTypes:
  - Ingress
  ingress:
  - {} # Allows ingress traffic from all sources (including outside the cluster if service is NodePort/LB)

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-pod1-to-pod2
  namespace: external
spec:
  podSelector:
    matchLabels:
      app: pod1-webapp # Apply this policy to Pod 1
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: internal
      podSelector:
        matchLabels:
          app: pod2-ssh # Allow egress only to Pod 2 in internal
    ports:
    - protocol: TCP
      port: 22 # Allow egress to Pod 2's SSH port

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-pod1-ingress-to-pod2
  namespace: internal
spec:
  podSelector:
    matchLabels:
      app: pod2-ssh # Apply this policy to Pod 2
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: external
      podSelector:
        matchLabels:
          app: pod1-webapp # Allow ingress only from Pod 1 in external
    ports:
    - protocol: TCP
      port: 22 # Allow ingress on the SSH port
